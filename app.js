const headerLinks = document.getElementsByClassName(".general-links");

headerLinks.addEventListener("mouseup", () => {
  headerLinks.style.color = red;
});

headerLinks.addEventListener("mousedown", () => {
  headerLinks.style.color = red;
});

const tryNow = (document.querySelector("#try").textContent = "new text");
